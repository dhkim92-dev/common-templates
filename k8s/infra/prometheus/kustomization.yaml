apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - base
configMapGenerator:
  - name: prometheus-config
    namespace: infra
    behavior: merge
    files:
      - prometheus.yml=prometheus.yaml
